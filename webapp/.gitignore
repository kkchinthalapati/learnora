# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright run artefacts. The persona suite writes a log, a JSON record and
# a screenshot per step — ~34MB per full run — which is evidence for a report,
# not source. The specs are tracked; what they emit is not.
test-results/
playwright-report/
tests/persona/out/
